<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="100dp"
    android:background="#2B2B2B"
    android:orientation="vertical"
    android:padding="10dp" >

    <!-- 右边上行 -->

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >


        
        <TextView
            android:id="@+id/simulation_check_title"
            style="@style/text_big_title"
            android:text="asdasda"
            android:textColor="#F7AB26"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/simulation_check_type"
            style="@style/text_small_tips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/simulation_check_level" />

        <TextView
            android:id="@+id/simulation_check_level"
            style="@style/text_small_tips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/simulation_check_title"/>
    </RelativeLayout>

    <!-- 右边下行 -->
    <!-- 右边下行END -->

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/simulation_check_part"
            style="@style/text_small_title" />
    </LinearLayout>

</LinearLayout>